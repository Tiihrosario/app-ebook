import{chapters,plan,checkItems}from'./content.js';import{load,save,erase,exportData,importData,localDate}from'./storage.js';
let state=load(),route=location.hash.slice(1)||'inicio';const root=document.querySelector('#app');const nav=[['inicio','Hoje'],['leitura','Jornada'],['hoje','Práticas'],['espaco','Perfil']];
const esc=s=>String(s).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));const commit=()=>{save(state);render()};
function shell(content){return `<div class="shell"><aside class="side"><div class="brand"><span class="brand-orbit" aria-hidden="true"></span><span>RAEM</span><small>O Código do Emagrecimento</small></div><nav class="nav" aria-label="Principal">${nav.map(([r,l])=>`<button class="nav-${r}" data-route="${r}" ${route===r?'aria-current="page"':''}><span class="nav-icon" aria-hidden="true"></span><span>${l}</span></button>`).join('')}</nav><p class="privacy-note">Dados salvos somente neste dispositivo.<br>Conteúdo educativo, sem diagnóstico.</p></aside><main class="main" id="main" tabindex="-1"><div class="topline"><span class="eyebrow">RAEM · reconhecer · arquitetar · executar · manter</span><span class="status" role="status">${navigator.onLine?'Salvo no dispositivo':'Offline · salvo no dispositivo'}</span></div>${content}</main></div>`}
function progress(){return Math.round(state.reader.completed.length/chapters.length*100)}
function home(){const next=chapters.find(c=>!state.reader.completed.includes(c.id))||chapters.at(-1);return shell(`<section class="home-hero"><div class="home-head"><div class="home-brand"><span class="brand-orbit" aria-hidden="true"></span><span>RAEM</span></div><p>Bom dia, Manu</p><button class="settings-btn" data-route="espaco" aria-label="Configurações">⚙</button></div><div class="hero-copy"><h1>O que faz<br>sentido para<br>você agora?</h1><p>Continue no seu ritmo.</p></div></section><section class="home-paper"><article class="continue-card"><span class="eyebrow">Continue de onde parou</span><h2>Capítulo ${next.id} · ${esc(next.title)}</h2><div class="progress-line"><span><i style="width:${progress()}%"></i></span><strong>${progress()}%</strong></div><button class="continue-btn" data-open="${next.id}">Continuar leitura <span aria-hidden="true">→</span></button></article><section class="moment"><h2>Para este momento</h2><div class="moment-grid"><button class="moment-card coral-moment" data-pause><span class="moment-symbol" aria-hidden="true">◯</span><span><strong>Fazer uma pausa</strong><small>Respire e volte para você.</small></span></button><button class="moment-card turquoise-moment" data-route="hoje"><span class="moment-symbol" aria-hidden="true">〰</span><span><strong>Registrar meu dia</strong><small>Escreva para se escutar.</small></span></button><button class="moment-card violet-moment" data-open="${next.id}"><span class="moment-symbol" aria-hidden="true">≋</span><span><strong>Ouvir um áudio</strong><small>Conteúdos para te inspirar.</small></span></button><button class="moment-card gold-moment" data-route="plano"><span class="moment-symbol orbit-small" aria-hidden="true"></span><span><strong>Ver ferramentas</strong><small>Recursos para sua jornada.</small></span></button></div></section></section>`) }
function reading(){return shell(`<header class="section-head"><span class="eyebrow">Biblioteca</span><h2>Oito capítulos, quatro movimentos.</h2><p class="lead">O conteúdo foi adaptado editorialmente da edição revisada do ebook. Nenhum capítulo funciona como teste, diagnóstico ou prescrição.</p></header><div class="chapter-list">${chapters.map(c=>`<article class="chapter-row"><span>${c.id}</span><div><small>${c.phase}</small><h3>${esc(c.title)}</h3><p>${esc(c.lead)}</p></div><button class="ghost" data-open="${c.id}">${state.reader.completed.includes(c.id)?'Revisitar':'Abrir'}</button></article>`).join('')}</div>`)}
function reader(id,sec=0){const c=chapters[id-1],s=c.sections[sec],audio=`assets/audio-${String(id).padStart(2,'0')}.mp3`;state.reader.chapter=id;state.reader.section=sec;save(state);return shell(`<article class="reader" style="--scale:${state.prefs.textScale}"><div class="toolbar" aria-label="Tamanho do texto"><button data-scale="-.1" aria-label="Diminuir texto">A−</button><button data-scale=".1" aria-label="Aumentar texto">A+</button></div><span class="eyebrow">${c.phase} · capítulo ${id} · ${sec+1}/${c.sections.length}</span><h2>${esc(sec===0?c.title:s[0])}</h2>${sec===0?`<p><em>${esc(c.lead)}</em></p><div class="audio"><label>Áudio do capítulo<audio controls preload="none" src="${audio}"></audio></label><button class="ghost" data-audio-cache="${audio}">Disponibilizar offline</button><small data-audio-status>Carregado somente quando você escolher.</small></div>`:''}<p>${esc(s[1])}</p><div class="reader-nav"><button class="ghost" data-reader-prev>Anterior</button>${sec===c.sections.length-1?`<button class="primary" data-complete="${id}">${state.reader.completed.includes(id)?'Etapa compreendida':'Marcar como compreendida'}</button>`:`<button class="primary" data-reader-next>Continuar</button>`}</div></article>`)}
function today(){const d=localDate(),rec=state.daily[d]||{checks:Array(4).fill(false),note:''};return shell(`<header class="section-head"><span class="eyebrow">Registro de ${new Date(d+'T12:00').toLocaleDateString('pt-BR')}</span><h2>Como está seu sistema hoje?</h2><p class="lead">Um registro de percepção, não uma nota sobre seu desempenho.</p></header><section class="grid"><div class="card coral" style="grid-column:span 2"><div class="check-grid">${checkItems.map((x,i)=>`<button class="check" data-check="${i}" aria-pressed="${rec.checks[i]}"><span>${rec.checks[i]?'●':'○'}</span><span>${esc(x)}</span></button>`).join('')}</div><label style="margin-top:16px">Uma observação opcional<textarea data-note placeholder="Escreva sem precisar concluir nada…">${esc(rec.note)}</textarea></label></div><aside class="card gold"><span class="eyebrow">Pausa guiada</span><h3>60 segundos de espaço</h3><p>Solte o ar devagar. Note corpo, emoção e contexto. Depois escolha entre comer, esperar, pedir apoio ou atender outra necessidade.</p><button class="primary" data-pause>Iniciar pausa</button></aside></section><section class="card violet" style="margin-top:16px"><span class="eyebrow">Plano Se–Então</span><h3>Antecipar sem controlar tudo.</h3><div class="grid"><label>Se acontecer…<input data-if placeholder="Ex.: eu chegar muito cansado"></label><label>Então posso…<input data-then placeholder="Ex.: pausar e escolher algo simples"></label><button class="primary" data-add-if>Adicionar</button></div><div class="if-list">${state.ifthen.map((x,i)=>`<div class="if-item"><span><strong>Se</strong> ${esc(x.if)}, <strong>então</strong> ${esc(x.then)}.</span><button class="ghost" data-del-if="${i}">Excluir</button></div>`).join('')}</div></section>`)}
function planPage(){const start=new Date(state.plan.start+'T12:00');return shell(`<header class="section-head"><span class="eyebrow">Continuidade</span><h2>Plano de 30 dias</h2><p class="lead">Use como sequência livre ou calendário real. Cada registro guarda a data em que foi feito.</p></header><div class="card"><div class="grid"><label>Modo<select data-plan-mode><option value="free" ${state.plan.mode==='free'?'selected':''}>Sequência livre</option><option value="calendar" ${state.plan.mode==='calendar'?'selected':''}>Calendário</option></select></label><label>Data inicial<input data-plan-start type="date" value="${state.plan.start}" ${state.plan.mode==='free'?'disabled':''}></label></div><p class="notice">Datas futuras ficam disponíveis para consulta, mas não podem ser marcadas antes de acontecer.</p><div class="plan-grid">${plan.map((x,i)=>{const due=new Date(start);due.setDate(due.getDate()+i);const dueKey=localDate(due),future=state.plan.mode==='calendar'&&dueKey>localDate(),done=!!state.plan.records[i];return `<button class="day ${done?'done':''} ${future?'future':''}" data-day="${i}" ${future?'disabled':''}><strong>${i+1}</strong><small>${state.plan.mode==='calendar'?due.toLocaleDateString('pt-BR',{day:'2-digit',month:'short'}):'Etapa'}</small><span class="sr">${esc(x)}</span><br><small>${done?'Registrado em '+new Date(done+'T12:00').toLocaleDateString('pt-BR'):'Abrir'}</small></button>`}).join('')}</div></div>`)}
function space(){return shell(`<header class="section-head"><span class="eyebrow">Privacidade e portabilidade</span><h2>Seus dados, sob seu controle.</h2><p class="lead">Não há conta, rastreamento, validação de compra ou sincronização automática neste pacote. Para vender com acesso restrito e uso entre dispositivos, será necessário conectar uma plataforma comercial e um backend.</p></header><div class="grid"><section class="card turquoise"><h3>Backup</h3><p>Exporte um arquivo compatível com esta versão.</p><button class="primary" data-export>Exportar</button></section><section class="card violet"><h3>Restaurar</h3><p>A importação valida versão, estrutura e limite de tamanho.</p><label class="ghost">Selecionar arquivo<input class="sr" type="file" accept="application/json" data-import></label></section><section class="card coral"><h3>Apagar</h3><p>Remove permanentemente os registros deste navegador.</p><button class="ghost" data-erase>Apagar dados</button></section></div><section class="card gold" style="margin-top:16px"><h3>Escopo e prontidão comercial</h3><p>Conteúdo educativo e de autorreflexão. Não diagnostica, trata ou prescreve. Antes da venda, publique Termos de Uso, Política de Privacidade, canal de suporte e política comercial; valide o conteúdo com profissionais habilitados e integre autenticação/licenciamento no ambiente de hospedagem.</p></section>`)}
function render(){root.innerHTML=route.startsWith('capitulo/')?reader(+route.split('/')[1],+route.split('/')[2]||0):route==='leitura'?reading():route==='hoje'?today():route==='plano'?planPage():route==='espaco'?space():home();bind();document.querySelector('#main')?.focus({preventScroll:true})}
function go(r){route=r;location.hash=r;render()}function bind(){document.querySelectorAll('[data-route]').forEach(b=>b.onclick=()=>go(b.dataset.route));document.querySelectorAll('[data-open]').forEach(b=>b.onclick=()=>go(`capitulo/${b.dataset.open}/0`));document.querySelector('[data-reader-next]')?.addEventListener('click',()=>go(`capitulo/${state.reader.chapter}/${state.reader.section+1}`));document.querySelector('[data-reader-prev]')?.addEventListener('click',()=>state.reader.section?go(`capitulo/${state.reader.chapter}/${state.reader.section-1}`):go('leitura'));document.querySelectorAll('[data-scale]').forEach(b=>b.onclick=()=>{state.prefs.textScale=Math.max(.8,Math.min(1.4,state.prefs.textScale+ +b.dataset.scale));commit()});document.querySelector('[data-complete]')?.addEventListener('click',e=>{const id=+e.currentTarget.dataset.complete;if(!state.reader.completed.includes(id))state.reader.completed.push(id);save(state);go(id<8?`capitulo/${id+1}/0`:'inicio')});document.querySelectorAll('[data-check]').forEach(b=>b.onclick=()=>{const d=localDate(),r=state.daily[d]||{checks:Array(4).fill(false),note:''};r.checks[+b.dataset.check]=!r.checks[+b.dataset.check];state.daily[d]=r;commit()});document.querySelector('[data-note]')?.addEventListener('change',e=>{const d=localDate(),r=state.daily[d]||{checks:Array(4).fill(false),note:''};r.note=e.target.value.slice(0,2000);state.daily[d]=r;save(state)});document.querySelector('[data-pause]')?.addEventListener('click',pause);document.querySelector('[data-plan-mode]')?.addEventListener('change',e=>{state.plan.mode=e.target.value;commit()});document.querySelector('[data-plan-start]')?.addEventListener('change',e=>{if(confirm('Alterar a data inicial mantém os registros já feitos, mas muda as datas previstas. Continuar?')){state.plan.start=e.target.value;commit()}else render()});document.querySelectorAll('[data-day]').forEach(b=>b.onclick=()=>{const i=+b.dataset.day;if(state.plan.records[i]){if(confirm('Remover este registro?'))delete state.plan.records[i]}else state.plan.records[i]=localDate();commit()});document.querySelector('[data-export]')?.addEventListener('click',()=>exportData(state));document.querySelector('[data-import]')?.addEventListener('change',async e=>{try{state=await importData(e.target.files[0]);save(state);alert('Backup restaurado.');render()}catch(err){alert(err.message)}});document.querySelector('[data-erase]')?.addEventListener('click',()=>{if(confirm('Apagar permanentemente todos os dados deste navegador?')){erase();state=load();go('inicio')}})}
function pause(){const m=document.createElement('div');m.className='modal';m.setAttribute('role','dialog');m.setAttribute('aria-modal','true');m.innerHTML='<div><span class="eyebrow">Pausa guiada</span><h2>Há espaço antes da próxima escolha.</h2><p data-count class="metric">60</p><p>Respire no seu ritmo. Observe o que está presente sem precisar resolver tudo agora.</p><button class="primary" data-close>Encerrar</button></div>';document.body.append(m);m.querySelector('[data-close]').focus();let n=60,t=setInterval(()=>{n--;m.querySelector('[data-count]').textContent=n;if(n<=0)clearInterval(t)},1000);m.querySelector('[data-close]').onclick=()=>{clearInterval(t);m.remove()}}
async function cacheAudio(e){const b=e.target.closest('[data-audio-cache]'),s=document.querySelector('[data-audio-status]');try{b.disabled=true;s.textContent='Baixando para uso offline…';const c=await caches.open('raem-audio-v5'),u=b.dataset.audio,r=await fetch(u);if(!r.ok)throw Error();await c.put(u,r);s.textContent='Áudio disponível offline neste dispositivo.';b.textContent='Disponível offline'}catch{s.textContent='Não foi possível baixar agora. Verifique a conexão.';b.disabled=false}}
root.addEventListener('click',e=>{if(e.target.closest('[data-audio-cache]'))return cacheAudio(e);if(e.target.closest('[data-add-if]')){const a=document.querySelector('[data-if]').value.trim(),b=document.querySelector('[data-then]').value.trim();if(!a||!b)return alert('Preencha as duas partes do plano.');state.ifthen.push({if:a.slice(0,240),then:b.slice(0,240)});commit()}const del=e.target.closest('[data-del-if]');if(del){state.ifthen.splice(+del.dataset.delIf,1);commit()}});
addEventListener('hashchange',()=>{route=location.hash.slice(1)||'inicio';render()});addEventListener('online',render);addEventListener('offline',render);if('serviceWorker'in navigator)addEventListener('load',()=>navigator.serviceWorker.register('./sw.js'));render();
